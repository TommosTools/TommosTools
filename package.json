{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "lerna run compile --stream",
    "lint": "lerna run lint --stream --parallel",
    "test": "lerna run test"
  },
  "devDependencies": {
    "eslint": "^8.33.0",
    "jest": "^29.4.1",
    "lerna": "^6.4.1",
    "typescript": "^4.9.5"
  }
}
